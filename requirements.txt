# Trinity Infinity Geometry — verification dependencies
#
# Install:   pip install -r requirements.txt
# Verify:    python verification/VERIFY_ALL.py
#
# Tested with Python >= 3.10.

numpy>=1.24       # array operations, eigendecomposition, integer ops
sympy>=1.12       # exact symbolic verification (D_4 Galois, Wedderburn projection, PSLQ)
mpmath>=1.3       # 50-digit precision (closed-form attractor, alpha=1/2 uniqueness)
matplotlib>=3.7   # optional: visualizations in 04_meta/figures/ and 02_results/*/figures/
