# Trinity Infinity Geometry — verification dependencies
#
# Install:   pip install -r requirements.txt
# Verify:    cd integers && python verify_integers.py   (and the other verify_*.py scripts)
#
# Tested with Python >= 3.10.

numpy>=1.24
