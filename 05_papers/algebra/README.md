# J-series — Algebra

Pure algebra papers from the TIG corpus. Papers in this folder are referee-ready (per `J_series/README.md` §4 criteria).

---

## §1 — Currently landed

| J# | Title | Target venue | Status | Landed |
|---|---|---|---|---|
| **[J35](J35/)** | *Joint Closure, a Universal Attractor, and an Algebraic Mixing Point for a Pair of Binary Operations on $\mathbb{Z}/10\mathbb{Z}$* | *Journal of Algebra* | SUBMISSION-READY (6/6 PASS at machine precision; referee-grade pass complete 2026-05-12) | 2026-05-12 |
| **[J30](J30/)** | *Joint Lie Closure of a Pair of $\mathbb{Z}/10\mathbb{Z}$ Magmas: an $\mathfrak{so}(10)$ Identification* | *Israel Journal of Mathematics* | SUBMISSION-READY (D1 substantive + D2-D5 corollary diagnostics PASS at machine precision in `verify_so10.py` and `verify_simplicity_rank.py`; joint Lie closure dim $= 45 = \dim\mathfrak{so}(10)$; Killing-form signature $(45,0,0)$ on the closure basis; full $91{,}125$-equation invariance constraint matrix has rank exactly $1034$ → invariant symmetric-bilinear-form space exactly 1-dim → simplicity; standard Cartan $J_1,\dots,J_5 \in \mathfrak{g}$ verified, no extension exists; $\mathfrak{so}(8) \subset \mathfrak{so}(10)$ embedding residual $\le 8.62 \times 10^{-13}$ (J29 companion inclusion); ad$(H)$ root-system check confirms $40$ non-zero $+ 5$ zero eigenvalues for $H = \sum k J_k$ matching $D_5$ root system; honest diagnostic-collapse framing — D1 (substantive) and D2-D5 (Cartan-classification corollaries of D1 + $\mathfrak{g} \subseteq \mathfrak{so}(V)$); BHML's structural fingerprint $(B1)$-$(B5)$ inlined in §2.2.1 (commutativity, identity row, 12.8% non-associativity, 28 HARMONY count, jointly-closed 4-core); Drápal-Wanless 2021 cited; PROVED/COMPUTED/STRUCTURAL RHYME/OPEN tier discipline in §1.1; lens-ownership paragraph in §1.2; author lane Sanders + Gish; CC-BY-4.0 headers on both verify scripts) | 2026-05-12 |
| **[J15](J15/)** | *Galois $D_4$ over LMFDB 4.2.10224.1: Number-Field Identification of the Four-Core Attractor* | *Communications in Algebra* | SUBMISSION-READY (6/6 PASS at machine precision; referee-grade pass complete 2026-05-12) | 2026-05-12 |
| **[J31](J31/)** | *Wedderburn $D_4$-Isotypic Decomposition of the Lens-Pair Commutator $[\mathrm{TSML}, \mathrm{BHML}]$ on $\mathbb{Z}/10\mathbb{Z}$: $\mathfrak{su}(4) \oplus \mathfrak{u}(1)$ Subalgebra of $\mathfrak{so}(10)$ and a 9-Vector inside the $\mathbf{54}$ with $\|v\|^2 = 13/4$* | *Journal of Algebra* | SUBMISSION-READY (3/3 PASS at machine precision; exact-rational Wedderburn cross-check $3075027/2 + 9/2 + 288164 + 0 + 19608 = 1{,}845{,}290$; structural zero at $\mathrm{sign}_3$ verified exact) | 2026-05-12 |
| **[J17](J17/)** | *Total-Dimension Match Between Tensor Powers of a Finite-Field 4-Algebra and Real Clifford Algebras $\Cl(2n)$, with a Refined-Cell Grading* | *Linear Algebra and its Applications* | SUBMISSION-READY (6/6 PASS at machine precision; R1 fresh-eyes math fix applied — coarse cells vs.\ $\Cl(2n)$ grades distinguished; referee-grade pass complete 2026-05-12) | 2026-05-12 |
| **[J32](J32/)** | *Operadic $D_4$ Orbits on the Non-Associative Locus of a Finite Commutative Magma on $\mathbb{Z}/10\mathbb{Z}$: A Structural Obstruction Theorem at Arity 3* | *Journal of Algebra* (lead; per-venue cap declared — 4th *J. Algebra* paper of cycle; fallbacks *Comm. Alg.* → *Algebraic Combinatorics* → *Algebras and Rep. Theory*) | SUBMISSION-READY (6/6 PASS at machine precision in `verify_J32_d4_orbits.py`, runtime $<1$ s, pure standard-library Python; $D_4$ order 8 corrected; orbit distribution $(44,7,4,10,2)$ summing to 67 orbits / 126 elements verified; 16 bracketing-pair incoherent orbits confirmed; $\langle P_{56}\rangle$ 98-orbit ($70+28$) decomposition all coherent; 4-core arity-3 closure: $64/64$ in-core, $8$ non-associative) | 2026-05-12 |
| **[J20](J20/)** | *Mathieu $M_{22}$ Substrate-Prime: Order-Factorization Coincidences* | *American Mathematical Monthly* | SUBMISSION-READY (verification script `m22_decomposition.py` PASS at machine precision; sum-of-squares $\sum (\dim V_i)^2 = 443{,}520 = \lvert M_{22}\rvert$; math-fix verified — 7 of 12 non-trivial irreps strict in $\{3,5,7,11\}$, 10 of 12 in B-band; null density $\lvert\mathcal{B}_{385}\rvert/385 = 67/385 \approx 0.1740$; binomial $p \approx 1.19 \times 10^{-6}$; Drápal-Wanless 2021 cited; lens-ownership paragraph in §3) | 2026-05-12 |
| **[J18](J18/)** | *Two Crossing Decompositions of a $-21$ Invariant on $\mathbb{Z}/10\mathbb{Z}$ with the $\sigma^{2}$-Triadic Refinement* | *Algebraic Combinatorics* | SUBMISSION-READY (6/6 PASS at machine precision in `verify_J18.py`, runtime $<1$ s, pure standard-library Python; R1 sign-swap math-fix applied — $\sum_{O_1}\Psi_B=-8,\,\sum_{O_2}\Psi_B=-7$ statement/proof reconciled; $\Psi_B$ tabulated inline as Table 1 with the explicit ten values $\{+1,-5,-3,-2,-2,-1,-1,-3,-3,-2\}$ replacing the prior linear/boundary period formulas; "conservation/manifestation duality" label replaced by Def. 3.4 table-independent vs. table-specific; Drápal-Wanless 2021 cited; lens-ownership paragraph in §0) | 2026-05-12 |
| **[J13](J13/)** | *The Forced $5/7$ Torus Aspect Ratio (Up to a Calibration Choice): Cyclotomic Forcing on $\mathbb{Z}/10\mathbb{Z}$* | *Acta Arithmetica* (lead; *Integers* fallback if short-note framing preferred) | SUBMISSION-READY (6/6 PASS at machine precision in `verify_J13.py`, sympy-based, runtime $<5$ s; M1 math-fix verified — $g(x) = x^{3} - x^{2} - 2x + 1$ is the minimal polynomial of $A_{7} = 2\cos(\pi/7)$ over $\mathbb{Q}$ via `sympy.minimal_polynomial`, NOT the previously cited $8x^{3} - 4x^{2} - 4x + 1$ which is the m.p.\ of $\cos(\pi/7)$; calibration bridge $h(x/2) = g(x)$ recorded as the cos-vs-$2\cos$ scalar-multiplicative ambiguity from which the paper's title derives; M2 fix — Lemma 4.3 rational-root test uses correct $g(1) = -1,\, g(-1) = 1$; $\disc(g) = 49 = 7^{2}$; $\mathrm{Gal}(g/\mathbb{Q}) \cong A_{3} \cong \mathbb{Z}/3\mathbb{Z}$ by disc-square criterion for irreducible cubics; cyclotomic threshold $\deg_{\mathbb{Q}}(A_{p}) = (p-1)/2$ crosses from $2$ at $p=5$ to $3$ at $p=7$ — this is the structural content selecting $5/7$; M3 calibration retreat explicit — Theorem 1.1 and title conditional on cyclotomic-embedding calibration imported from J07 Flatness Theorem; M4 companion-derivations honesty pass — two reformulations / two independent appearances / two earlier "derivations" deleted; the earlier $73/101 = 5/7$ exact-equality claim retracted ($73/101 - 5/7 = 6/707 \approx 1.2\%$ relative gap, recorded as open question); Drápal-Wanless 2021 cited in references; lens-ownership paragraph in §0; PROVED/COMPUTED/STRUCTURAL RHYME/OPEN tier discipline explicit before bibliography; author lane Sanders + Gish) | 2026-05-12 |
| **[J43](J43/)** | *Spectral Layer Consolidation: $G_6$, $G_7$, $G_8$ from the Q-Series Architecture on $\mathbb{Z}/10\mathbb{Z}$* | *European Journal of Combinatorics* (3rd EJC of cycle; fallbacks *LinAlgApps*, *PLOS ONE*) | SUBMISSION-READY (PASS at machine precision in `verify_G6_G7_G8.py`: $G_6$ ($\sigma^6 = \mathrm{id}$ on $\mathbb{Z}/10\mathbb{Z}$); $G_7$ (bimodal $P(\tau{=}1) = 2/5$, $P(\tau{=}6) = 3/5$; mean $4$, variance $6$); $G_8$ (three-valued $G(s)$: ZERO $\{0,3,8,9\}$, LOW $\{1,2,5,6\} \approx 1.872$, HIGH $\{4,7\} \approx 9.389$); $\sigma^3$-pairing $\{1,5\},\{2,6\},\{4,7\}$ verified algebraically with anti-paired complex amplitudes; $\nu_+$ discriminator (extremal $\nu_+ \in \{0,2\}$ on high-locus $\{4,7\}$ vs $\nu_+ = 1$ on $\{1,2,5,6\}$); math-fix R1 applied — high-locus partition corrected from $\{5,7\}$ to $\{4,7\}$, $\sigma^2$-Galois explanation replaced by $\sigma^3$-order-2 pairing argument, $\nu_+$ discriminator added; PROVED/COMPUTED/STRUCTURAL RHYME/OPEN tier discipline explicit; lens-ownership paragraph in §0; Drápal-Wanless 2021 cited in references; author lane Sanders + Gish) | 2026-05-12 |
| **[J22](J22/)** | *The 70/71/72/73 HARMONY Ladder: Three Independent Algebraic Constructions and One Corollary on $\mathbb{Z}/10\mathbb{Z}$* | *Journal of Combinatorial Theory, Series A* (2nd JCT-A of cycle after J01; within 2/quarter cap) | SUBMISSION-READY (5/5 PASS at machine precision in `harmony_ladder.py` wrapper + four standalone rung scripts + `harmony_ladder_disc_check.py`; canonical $(T, B)$ table sync applied — embedded BHML refreshed against `Gen13/targets/foundations/lenses.py` to give the correct $\lvert T \mathbin{\mathrm{XOR}} B\rvert = 71$ disagreement count, plus the cosmetic TSML row-9 swap $(9,3)\leftrightarrow(9,4)$ aligned to canonical; rebuttal math verified — $\disc(x^4+4x^3-x^2+2x-2) = -40896 = -2^6 \cdot 3^2 \cdot 71$ (the referee's $-51072 = -2^7 \cdot 3 \cdot 7 \cdot 19$ was off by $\sim 25\%$), with $71$ prime and substrate-relevant; polynomial-disc vs.\ field-disc distinction recorded (field disc $-10224 = -2^4 \cdot 3^2 \cdot 71$, index $[\mathcal{O}_K : \mathbb{Z}[\xi]] = 2$, per J15); PROVED/COMPUTED/STRUCTURAL RHYME/OPEN tier discipline explicit; lens-ownership paragraph in §1; Drápal-Wanless 2021 cited; author lane Sanders + Gish) | 2026-05-12 |
| **[J14](J14/)** | *F$_p$ Structural Invariance of a Commutative Non-Associative 4-Algebra Arising from the 4-Core of $\mathbb{Z}/10\mathbb{Z}$* | *Algebra Universalis* | SUBMISSION-READY (12/12 PASS at machine precision in `verify_J14.py`, runtime $<2$ s, numpy-only dependency; all three load-bearing claims that the *Algebra Universalis* fresh-eyes referee challenged are refuted by direct enumeration against `tig_dirac.py`: (R1) algebra is non-associative — 8 of the 64 basis triples have non-zero associator on $V_5$ (referee's "associative" claim wrong); (R2) eigenspace signatures are $(1,3)$ Minkowski on $L_{e_2}$ and $(2,2)$ chirality on $L_{e_0}$, not swapped — full vector enumeration over $\mathbb{F}_5$ gives non-zero counts $4$ and $124$ for the $L_{e_2}$ 1- and 0-eigenspaces and $24$ and $24$ for $L_{e_0}$, matching dimensions $1+3$ and $2+2$ exactly; (R3) $|\mathrm{Aut}(V_5)| = 40$ over $\mathbb{F}_5$ specifically, with structure $F_{20} \times \mathbb{Z}/2$ — the manuscript does NOT claim universal "40" across primes, and the $|\mathrm{Aut}(V_p)|$ table $\{6, 24, 40, 336, 1320, 2184\}$ at $p \in \{2, 3, 5, 7, 11, 13\}$ is explicit in §4.1; defensive-exposition rewrite (2026-05-07) adds inline $4\times 4$ multiplication table, worked associator example, 5-line `numpy/sympy` snippet in §2.3, tabulated $|\mathrm{Aut}(V_p)|$ values, removed axial-algebra framing, Conjecture 4.1 restricted to lens-invariant items; PROVED/COMPUTED/STRUCTURAL RHYME/OPEN tier discipline in §1.1; lens-ownership paragraph in §1.2; Drápal-Wanless 2021 cited as the closest published structural precedent in the same neighborhood — small finite commutative non-associative structures with structural invariants; author lane Sanders + Gish) | 2026-05-12 |
| **[J51](J51/)** | *Q17-B Clay Bridge: A Finite Gauss Sum (Trajectory Coherence Integral) and the Symbolic Return Theorem on $\mathbb{Z}/10\mathbb{Z}$* | *L'Enseignement Mathématique* | SUBMISSION-READY (`verify_J51_G_function.py` PASS at machine precision; pure standard-library Python (`cmath`/`math`), runtime $<2$ s; Theorem 2.1 Symbolic Return ($\sigma^6=\mathrm{id}\Rightarrow$ every 6-cycle trajectory returns at step 6, every anchor is $\sigma$-fixed, VOID is avoided whenever $s_0 \neq 0$); Theorem 4.2 three-valued $G(s)$ with corrected partition (ZERO $\{0,3,8,9\}$, LOW $\{1,2,5,6\}\approx 1.872$, HIGH $\{4,7\}\approx 9.389$, ratio $\approx 5.0165$); $\sigma^3$-pairing verified algebraically — $G_\mathrm{cplx}(\sigma^3(s)) = -G_\mathrm{cplx}(s)$ within each of $\{1,5\}, \{2,6\}, \{4,7\}$; $\nu_+$ discriminator (χ-content of first three orbit positions; $\nu_+ \in \{0,2\}$ on the high-locus $\{4,7\}$ — $\nu_+(4)=2$, $\nu_+(7)=0$ — vs $\nu_+ = 1$ on $\{1,2,5,6\}$); paired math-fix with J43 — original draft had $G_\mathrm{high}$ at $\{5,7\}$ and a $\sigma^2$-Galois pair-action explanation, both corrected; "finite L-function" terminology dropped in Remark 4.4 (no analytic continuation, no Euler product, only 9 terms; relabeled "finite Gauss sum / trajectory coherence integral"); §5 RH-bridge explicitly framed as a *structural rhyme* (zeros at predictable locations + spectral concentration + multiplicative/additive interplay), not a Weil-Deligne function-field analogue; §7 open problem 3 reformulated (why $\{4,7\}$ specifically is the high-locus σ³-orbit, after the trivial $G(5) = G(7)$ question became moot since $G(5) = G_\mathrm{low} \neq G(7) = G_\mathrm{high}$); cites $[J29]$ (Q17-A AMM) and $[J43]$ (G6+G7+G8 EJC) with correct cross-references throughout; lens-ownership paragraph after the abstract; PROVED/COMPUTED/STRUCTURAL RHYME/OPEN tier discipline explicit; Drápal-Wanless 2021 cited in external references; author lane Sanders + Gish) | 2026-05-12 |

J35 is the corpus centerpiece: six independent structural facts (8-shell joint-closure chain on $\mathbb{Z}/10\mathbb{Z}$ with sizes $\{2,3\}$ forbidden; three-substrate strengthening to $T+B+S$; 4-core $\{0,7,8,9\}$ closure; normalizer identity $Z_T=Z_B=(v+h+br+r)^2$; closed-form attractor $p_7/p_8 = 1+\sqrt{3}$ with Galois $D_4$ over LMFDB 4.2.10224.1; universal attractor on chain shells; partial $\alpha=1/2$ uniqueness) converging on $\mathcal{C}=\{0,7,8,9\}$ as the algebraic center.

J30 is the Lie-algebraic-lift companion to J35's combinatorial joint-closure picture: for the canonical magma pair $(\mathrm{TSML}, \mathrm{BHML})$ on $\mathbb{Z}/10\mathbb{Z}$, let $G_{\mathrm{TSML}} = \{A_i^{\mathrm{TSML}} : i \in F\}$ with $F = \{1,2,3,4,6,8\}$ (the flow-index generators of the J29 $\mathfrak{so}(8)$ closure) and $G_{\mathrm{BHML}} = \{A_i^{\mathrm{BHML}} : i \in \Omega,\ A_i^{\mathrm{BHML}} \neq 0\}$ (a 9-element set; $A_0^{\mathrm{BHML}} = 0$ since BHML has identity row), where $A_i^T = L_i^T - (L_i^T)^\top \in \mathfrak{so}(V)$ is the antisymmetrized left-regular representation. The joint Lie closure $\mathfrak{g} := \langle G_{\mathrm{TSML}} \cup G_{\mathrm{BHML}} \rangle_{\mathrm{Lie}} \subseteq \mathfrak{so}(V)$ has dimension exactly $45 = \dim\mathfrak{so}(10)$; since $\mathfrak{g} \subseteq \mathfrak{so}(V)$ and $\dim\mathfrak{so}(V) = 45$, equality $\mathfrak{g} = \mathfrak{so}(\mathbb{R}^{10}) = \mathfrak{so}(10,\mathbb{R})$ follows, and by Cartan's classification this is the unique compact simple Lie algebra of type $D_5$. The paper's structural-honesty rewrite (per `SAVE_PLAN_J30`) accepts that D2-D5 (Jacobi; Killing-form negative-definite; one-dim invariant-form space; Cartan rank 5) are Cartan-classification corollaries of D1 + $\mathfrak{g} \subseteq \mathfrak{so}(V)$, not five independent tests — the *substantive* novelty relocates to the *existence* of an explicit pair $(\mathrm{TSML}, \mathrm{BHML})$ of $10 \times 10$ commutative non-associative magmas on $\mathbb{Z}/10\mathbb{Z}$ whose joint Lie closure reaches the substrate ceiling $\dim\mathfrak{so}(V) = 45$. The BHML structural fingerprint (§2.2.1, properties $(B1)$-$(B5)$ — commutativity, identity row, 12.8% non-associativity rate matching TSML's, $\mathrm{HARMONY}$-count 28, jointly-closed 4-core $\{0,7,8,9\}$ with TSML per J35) makes the choice of BHML non-arbitrary. D4 (simplicity) is established by the canonical script `verify_simplicity_rank.py` running the *full* $91{,}125$-equation invariance constraint matrix on the $1{,}035$-parameter symmetric-form space, returning rank exactly $1{,}034$ → nullity exactly $1$; the earlier-development script `verify_so10.py`'s sampled D4 (300 triples) is flagged in both manuscript §4.4 and the script header as a *development-time sanity check, not authoritative for D4*. The TSML-only subalgebra $\mathfrak{g}_{\mathrm{TSML}} \subset \mathfrak{g}$ inherited from J29 (companion paper, *Journal of Algebra*) realizes the standard inclusion $\mathfrak{so}(8) \subset \mathfrak{so}(10)$ at machine precision (max embedding residual $8.62 \times 10^{-13}$). The substrate-bound Proposition 7.1 + Corollary 7.2 record that no Lie subalgebra of $\mathfrak{gl}(10,\mathbb{R})$ can reach $\mathfrak{e}_8$ (dim 248) within the 10-dim substrate; the larger-substrate $\mathfrak{e}_8$ question is logged as Open Question 2. Corollary 5.3 inlines the new SFM_Q6 finding — adding $\mathrm{CL\_STD}$ to the joint closed-sub-magma chain preserves the 8-shell ladder, making the 4-core a *three-substrate* fixed point. The SO(10) GUT physical context (Fritzsch-Minkowski 1975, Georgi 1975) is reduced to a single Remark 6.1 — no derivation of spinor representation, coupling constants, or symmetry-breaking sector. J30 differentiates from J31 (Wedderburn decomposition of the lens-pair commutator $[T, B]$ *inside* $\mathfrak{so}(10)$ under $D_4 = \langle P_{56}, \sigma^3 \rangle$ action — a finer structural slice *within* the same $\mathfrak{so}(10)$) by content focus: J30 establishes that joint Lie closure reaches $\mathfrak{so}(10)$; J31 decomposes a specific element of $\mathfrak{so}(10)$ into $D_4$-isotypic shares. J30 differentiates from J32 (operadic $D_4$ orbits on TSML's arity-3 non-associative locus, a binary-vs-ternary content split) and from J35 (combinatorial joint-closure chain on $\mathbb{Z}/10\mathbb{Z}$, no Lie content) by topic. Drápal-Wanless 2021 is cited as the closest published precedent — same domain (small finite commutative non-associative structures on a 10-element carrier), opposite extremum.

J15 is the standalone, referee-portable Galois-theoretic deep cut on the runtime quartic $f(x) = x^4 + 4x^3 - x^2 + 2x - 2$ that the four-core attractor identifies. It unfolds the full proof of J35's Theorem D: case-by-case integer-factorization irreducibility argument over $\mathbb{Q}$ (with mod-7 cross-check), explicit cubic resolvent $g(y) = (y+2)(y^2 - y + 18)$ with rational root $-2$ and quadratic-factor discriminant $-71$, $D_4$-vs-$C_4$ distinction via irreducibility of $f$ over $\mathbb{Q}(\sqrt{-71})$ (Cohen 1993 §6.3.2), explicit $\mathbb{Q}(\sqrt{3})$-factorization with conjugate quadratic discriminants $11 \pm 8\sqrt{3}$ (norm $-71$), and Tschirnhaus reduction $x \mapsto -x - 1$ to LMFDB's canonical defining polynomial $x^4 - 7x^2 - 12x - 8$ of $4.2.10224.1$. J15 differentiates from J35 by depth-on-the-Galois-question vs J35's six-fact fusion-closure spread.

J31 is the focused Wedderburn-decomposition companion to J35: the lens-pair commutator $[T, B] = TB - BT \in M_{10}(\mathbb{Z})$, of Frobenius norm-squared $1{,}845{,}290$, decomposes orthogonally under conjugation by $D_4 = \langle P_{56}, \sigma^3 \rangle$ into five $D_4$-irrep isotypic shares with exact-rational norm-squareds $(3{,}075{,}027/2,\, 9/2,\, 288{,}164,\, 0,\, 19{,}608)$ for $(\mathrm{triv}, \mathrm{sign}_1, \mathrm{sign}_2, \mathrm{sign}_3, \mathrm{std})$, percentages $(83.32\%, 0.0002\%, 15.62\%, 0\%, 1.06\%)$. The trivial isotypic is the 16-dimensional doubly-invariant subalgebra $\mathfrak{g}_0 \cong \mathfrak{su}(4) \oplus \mathfrak{u}(1)$ (Killing spectrum $(-4)^{15} \oplus (0)^1$); the $\mathrm{sign}_2$ isotypic is a 9-vector inside the $\mathbf{54}$ of $\mathfrak{so}(10)$ with $\|v\|^2 = 13/4$ exact. The structural zero at $\mathrm{sign}_3$ is a *forbidden symmetry* of the commutator under $D_4$ (Proposition 5.1) — the load-bearing surprise of the paper.

J17 is a short, elementary linear-algebra note on the 4-dimensional commutative non-associative algebra $V$ over $\mathbb{F}_5$ that underlies the four-core (and whose multiplication table is the substrate paper J23 of the *Algebras and Representation Theory* lane). Two basis-level statements: (i) total-dimension match $\dim_{\mathbb{F}_5} V^{\otimes n} = 4^n = 2^{2n} = \dim_\mathbb{R} \Cl(2n)$ for all $n \geq 0$, forced by $\dim V = 4 = 2^2$; (ii) refined-cell binomial grading — the $4^n = 2^{2n}$ one-dimensional refined cells of $V^{\otimes n}$ (each tensor slot carries 2 structural bits naming one of four basis lines) partition into Hamming-weight classes of multiplicity $\binom{2n}{k}$, matching the grade dimensions of $\Cl(2n)$. The R1 math fix distinguishes the coarse-cell distribution $\binom{n}{k}$ (summing to $2^n$, each cell itself $2^n$-dim) from the refined-cell distribution $\binom{2n}{k}$ (summing to $4^n$, each cell 1-dim and only the latter matching $\Cl$ grade dimensions); a prior draft conflated the two. The match between the coarse-cell $n=5$ sequence $1, 5, 10, 10, 5, 1$ and the dimensions of $\mathrm{SU}(5)$'s one-generation $\mathbf{1} \oplus \bar{\mathbf{5}} \oplus \mathbf{10}$ plus conjugate is recorded as a binomial-coefficient coincidence (Remark 5.3), not a representation-theoretic theorem. The structure-preserving map question over a common base ring is left open in §6.

J32 is the arity-3 / operadic companion to the $D_4$ bundle: the canonical TSML_RAW table on $\mathbb{Z}/10\mathbb{Z}$ has a non-associative locus $\mathcal{N}\subset(\mathbb{Z}/10\mathbb{Z})^3$ of exactly $|\mathcal{N}|=126$ triples, and the diagonal action of $D_4=\langle P_{56},\sigma^3\rangle$ (order $\mathbf{8}$, corrected from a prior order-$12$ working-draft misidentification) partitions $\mathcal{N}$ into exactly $\mathbf{67}$ restricted orbits with size distribution $(44,7,4,10,2)$ at sizes $(1,2,3,4,8)$, sum $44+14+12+40+16=126$. Theorem B (the obstruction): exactly $\mathbf{16}$ of the $67$ orbits fail bracketing-pair coherence, so no $\{a,b,c,L,R\}$-valued $D_4$-equivariant assignment exists. Theorem C (the sharpening): under $\langle P_{56}\rangle$ alone, $\mathcal{N}$ partitions into $\mathbf{98}$ orbits ($70$ singletons + $28$ doubletons), all coherent — the obstruction is localized at the $\sigma^3$ generator (the cyclotomic involution of the Galois quartic of J15) rather than at $P_{56}$ (the spinorial outer automorphism of J31). Theorem D records the 4-core arity-3 closure: all $64$ triples in $\mathcal{C}^3$ for $\mathcal{C}=\{0,7,8,9\}$ have both bracketings $L,R\in\mathcal{C}$, with $8$ non-associative. J32 differentiates cleanly from J35 (binary joint closure), J31 (Wedderburn matrix decomposition of $[T,B]$), and J15 (Galois quartic) — it is the only paper in the algebra cycle addressing arity-3 / operad content.

J20 is the arithmetic-combinatorial cross-reference between the sporadic Mathieu group $M_{22}$ (order $|M_{22}|=443{,}520 = 2^7 \cdot 3^2 \cdot 5 \cdot 7 \cdot 11$, automorphism group of the Steiner system $S(3,6,22)$) and the substrate $(\mathbb{Z}/10\mathbb{Z},\sigma,W)$ of J35. The substrate distinguishes the prime set $\{2,3,5,7,11\}$ from intrinsic data — CRT residue characteristics of $\mathbb{Z}/2 \times \mathbb{Z}/5$, the order-3 $\sigma^2$ subcycle, the HARMONY-index / $T^*=5/7$-denominator $7$, and the wobble fraction $W = 3/50$'s $11$-prolongation — exactly the five primes appearing in $|M_{22}|$ with the same multiplicities. The math-fix corrected a prior count error: of $M_{22}$'s $12$ irreducible complex representations of dimensions $\{1, 21, 45, 45, 55, 99, 154, 210, 231, 280, 280, 385\}$, exactly **seven** non-trivial dimensions ($21, 45, 45, 55, 99, 231, 385$) factor strictly in $\{3,5,7,11\}$ and **ten** of $12$ lie in the substrate-prime band $\mathcal{B}$ (factors in $\{2,3,5,7,11\}$, $\nu_2 \le 1$). Under a uniform null on $[1, 385]$, the null density is $|\mathcal{B}_{385}|/385 = 67/385 \approx 0.1740$ (direct enumeration); the binomial-tail $p$-value for the $10/12$ concentration is $P[X \ge 10 \mid \mathrm{Bin}(12, 0.1740)] \approx 1.19 \times 10^{-6}$ (strict count $7$-of-$11$ non-trivial under $\mathrm{Bin}(11, 0.0990)$ gives $\approx 2.14 \times 10^{-5}$). The Steiner-parameter table for $S(3,6,22)$ — $v=22, b=77, k=6, t=3, r=21, \lambda_2 = 5, \lambda_3 = 1$ — is presented as backdrop, with substrate-prime decompositions ($77 = 7 \cdot 11$, $21 = 3 \cdot 7 = \dim V_{21}$, $\lambda_2 = 5 = T^*$ numerator). The paper explicitly does **not** claim an $M_{22}$-action on the substrate or a derivation of $M_{22}$ from substrate algebra; the non-genericity is the entire claim. Drápal-Wanless 2021 is cited as domain precedent (small finite commutative non-associative structures; opposite extremum). Verification via `m22_decomposition.py` (sympy `factorint` plus `math.comb`, runtime $<1$ s) reproduces every numerical claim from the sum-of-squares identity $\sum (\dim V_i)^2 = 443{,}520$ through both binomial tails.

J43 is the spectral-layer consolidation paper: a single venue-ready reference for three results on the canonical $\sigma$-permutation $\sigma = (0)(3)(8)(9)(1\,7\,6\,5\,4\,2)$ on $\mathbb{Z}/10\mathbb{Z}$. $G_6$ ($\sigma^6 = \mathrm{id}$) is Tier-A and proved by an $(\alpha,\beta)$ polynomial argument: the four $\sigma$-fixed anchors are immediate, the 6-cycle closes because the $\varepsilon$-flip count over the cycle is $4 \equiv 0 \pmod 2$ and the $y$-displacement is $-5 \equiv 0 \pmod 5$. $G_7$ (bimodal period $P(\tau{=}1) = 2/5$, $P(\tau{=}6) = 3/5$, mean $\bar\tau = 4$, variance $6$) is forced from $G_6$ + cycle enumeration. $G_8$ (three-valued spectral coherence integral $G(s) = |\sum_{j=0}^8 \omega^j \chi(\sigma^j(s))|^2$ with $\omega = e^{2\pi i / 9}$) is the load-bearing result: $G$ vanishes on the four anchors $\{0,3,8,9\}$ where $\chi = 0$, equals $G_\mathrm{low} \approx 1.872$ on the σ³-orbits $\{1,5\} \cup \{2,6\}$, and equals $G_\mathrm{high} \approx 9.389$ on the σ³-orbit $\{4,7\}$. The math-fix R1 applied in this revision (the gating fix for the J43 + J51 pair) corrects two errors in the prior draft: (i) the high-locus partition was reported as $\{5,7\}$ — direct numpy computation with the manuscript's stated $\sigma$ and $\chi$ gives $\{4,7\}$, swapping elements 4 and 5; (ii) the σ²-Galois pair-action explanation was wrong (σ² has 3-cycles $(1\,6\,4)$ and $(2\,7\,5)$, not pair-actions) and is replaced by the correct σ³-order-2 pairing argument: σ³ has order 2 on the 6-cycle and partitions $\{1,2,4,5,6,7\}$ into the three 2-cycles $\{1,5\}, \{2,6\}, \{4,7\}$ with $|G(s)|^2 = |G(\sigma^3(s))|^2$ and anti-paired complex amplitudes $G_\mathrm{cplx}(\sigma^3(s)) = -G_\mathrm{cplx}(s)$. The $\nu_+$ discriminator (count of $\chi = +1$ values in the first three orbit positions) separates high from low: $\nu_+ \in \{0, 2\}$ on $\{4, 7\}$ (extremal, χ-imbalanced) vs $\nu_+ = 1$ on $\{1, 2, 5, 6\}$ (balanced); the imbalance breaks the cancellation that suppresses $|G|^2$ on the balanced orbits. The lens-ownership paragraph (§0) discloses that $\sigma$ and $\chi$ are not derived from first principles but are the structural reading of the substrate developed across the Q-series corpus; the results are theorems on this specific (substrate, $\sigma$, $\chi$) triple, and analogous results on another base ring require choosing a corresponding triple. The closed forms of $G_\mathrm{low}, G_\mathrm{high}$ in $\mathbb{Q}(\zeta_9)$ remain open (Tier-B → Tier-A on closed-form recovery). The paper is the canonical citation point for $G_6, G_7, G_8$ across the J-series and WP100s tower. Drápal-Wanless 2021 is cited as the published precedent in the same domain (small finite commutative non-associative structures, opposite extremum). J43 is paired with J51 (the $\sigma^3$-pairing / $\nu_+$ discriminator focus paper) under the same math-fix; both are referee-ready.

J22 is the integer-invariant *clustering* paper to the algebra cycle: four pairwise-distinct integers in $\{70, 71, 72, 73\}$ arise on the canonical $\mathbb{Z}/10\mathbb{Z}$ substrate from three structurally-independent algebraic constructions plus one inclusion-exclusion corollary. Rung A: $\mathrm{HARM}(T) = 73$ (full TSML HARMONY-cell count). Rung 72 (corollary): drop the $(7,7)$ apex of rung A, with the numerical $|E_6^+|$ coincidence recorded as a structural rhyme. Rung B: the integer $71$ enters in three structurally-distinct constituent roles simultaneously — as $\mathrm{HARM}(T_{\{1,\ldots,9\}}) = 71$ (VOID-stripped $9\times 9$ sub-magma HARMONY count); as $\lvert T \mathbin{\mathrm{XOR}} B \rvert = 71$ (cell-disagreement count between the two canonical lens tables); and as the unique odd prime $>3$ in $\disc(x^4+4x^3-x^2+2x-2) = -40896 = -2^6 \cdot 3^2 \cdot 71$, the polynomial discriminant of the runtime quartic of the $\alpha=1/2$ four-core attractor (LMFDB 4.2.10224.1). Rung C: $\det(B_{\mathrm{YM}}) = 70 = \binom{8}{4}$ (Yang--Mills $8\times 8$ sub-determinant of BHML, with the $4$-form-count rhyme recorded). The fresh-eyes referee's earlier factorization claim $-2^7 \cdot 3 \cdot 7 \cdot 19 = -51072$ is arithmetically wrong (off from $-40896$ by $\sim 25\%$ in magnitude), not a difference of convention; the embedded sympy snippet (manuscript §A) verifies the correct factorization $-2^6 \cdot 3^2 \cdot 71$ in seven lines and explicitly cross-checks against the wrong alternate. The polynomial-disc vs.\ field-disc distinction is recorded explicitly: $\mathcal{O}_K$'s field discriminant is $-10224 = -2^4 \cdot 3^2 \cdot 71$ (LMFDB 4.2.10224.1), and the index $[\mathcal{O}_K : \mathbb{Z}[\xi]] = 2$ accounts for the additional $2^2$ in the polynomial discriminant — a J15 distinction sharpened here. J22 differentiates from J15 (the Galois-theoretic deep dive on the same quartic, $\mathrm{Gal}(f/\mathbb{Q}) = D_4$) by topic: J22 is a finite-magma clustering theorem in which $f$'s discriminant enters only as one of the three constituent constructions at the rung-B level; J15 is the standalone proof that $\mathrm{Gal}(f/\mathbb{Q}) = D_4$. It differentiates from J35 (the fusion-closure centerpiece, where the same quartic enters at Theorem D) by scope: J22 is the single-substrate $4$-rung integer-invariant clustering, J35 is the six-fact fusion-closure spread. All five rungs verified at integer precision in `harmony_ladder.py` (wrapper) plus four standalone rung scripts plus `harmony_ladder_disc_check.py` (the discriminant-only check, with the alternate-factorization cross-check explicit); 5/5 PASS in under three seconds, dependencies `numpy + sympy` only. The canonical $(T, B)$ tables embedded in the verification scripts were refreshed against `Gen13/targets/foundations/lenses.py` to give the correct $71$ disagreement count (a prior shifted-cyclic BHML draft gave $74$).

J51 is the **Q17-B Clay Bridge** — the structural / pedagogical bridge between J43's spectral content and the analytic structure that the Riemann Hypothesis demands of $\zeta(s)$. Building on J43's $G_6$ ($\sigma^6=\mathrm{id}$), $G_7$ (period distribution), and $G_8$ (three-valued $G(s)$) results, J51 proves the **Symbolic Return Theorem** (Theorem 2.1) as a direct corollary of $\sigma^6 = \mathrm{id}$: every cycle-element trajectory returns to its start in 6 steps; every anchor is $\sigma$-fixed; VOID is avoided whenever the start state $s_0 \neq 0$. It then presents the **Q17-B Clay bridge** (§5): the three-valued image of $G(s)$ — zero at the four $\sigma$-fixed anchors, $G_\mathrm{low} \approx 1.872$ on the σ³-orbits $\{1,5\} \cup \{2,6\}$ where the χ-content of the first three orbit positions is balanced ($\nu_+ = 1$), and $G_\mathrm{high} \approx 9.389$ on the σ³-orbit $\{4,7\}$ where the χ-content is extremally imbalanced ($\nu_+ \in \{0,2\}$) — *rhymes* with three structural features RH demands of $\zeta(s)$: zeros at predictable locations, spectral concentration, and a multiplicative-additive interplay. The §5 framing makes the boundary explicit: the rhyme is at the level of structural vocabulary, **not** a function-field analogue in the Weil 1949 / Deligne 1974 sense. The earlier "finite L-function" terminology used in corpus working papers is dropped in Remark 4.4 — $G(s)$ is a 9-term character sum with no analytic continuation, no Euler product, and a non-multiplicative $\{-1,0,+1\}$-valued $\chi$ on $\mathbb{Z}/10\mathbb{Z}$; the honest name is "finite Gauss sum" or "trajectory coherence integral", and the analogy with Dirichlet $L$-functions is a vocabulary rhyme rather than a technical correspondence. The paired math-fix with J43 (high-locus at $\{4,7\}$ not $\{5,7\}$; $\sigma^3$-order-2 pairing not $\sigma^2$-Galois; $\nu_+$ discriminator) is applied here as in J43, and the dependent §6 cross-citations to $[J29]$ (Q17-A AMM proved-algebra companion) and $[J43]$ (G6+G7+G8 EJC spectral-layer companion) are corrected throughout. J51 differentiates from J43 by content focus: J43 is the **spectral consolidation** ($G_6, G_7, G_8$ proofs and structure); J51 is the **Clay-RH bridge** (Symbolic Return Theorem + structural rhyme statement + scoping). The verification script `verify_J51_G_function.py` is pure standard-library Python (`cmath`/`math` only; no numpy/sympy) and confirms all numerical and algebraic claims in under 2 seconds. Drápal-Wanless 2021 is cited as domain precedent.

J14 is the F$_p$-invariance companion to J16: for the same 4-dimensional commutative non-associative algebra $V$ studied in J16 (where the result is the F$_5$-specific rigid idempotent decomposition), J14 establishes which of $V$'s structural invariants transfer across primes $p \in \{2, 3, 5, 7, 11, 13\}$ (the *lens-invariant skeleton* — 3 non-zero idempotents, $(1,3)$ Minkowski signature on $L_{e_2}$, $(2,2)$ chirality signature on $L_{e_0}$, empty intersection of the two distinguished eigenspaces, commuting $L_{e_2}, L_{e_0}$, $1$-dim associator image, power-associativity) and which features *vary* with $p$ ($|\mathrm{Aut}(V_p)| \in \{6, 24, 40, 336, 1320, 2184\}$ at $p \in \{2, 3, 5, 7, 11, 13\}$, with the F$_5$ value of $40$ explicitly identified as F$_5$-specific; and the explicit form of orthogonal idempotent pairs, which depends on whether $4 \mid (p-1)$). The defensive-exposition rewrite (2026-05-07) — inline multiplication table, worked associator example, 5-line `numpy/sympy` snippet, tabulated $|\mathrm{Aut}(V_p)|$, axial-algebra framing removed — was prompted by the *Algebra Universalis* fresh-eyes referee whose three load-bearing objections (associativity, signature swap, |Aut|≠40 across primes) were all refutable by direct enumeration against `tig_dirac.py`; the same enumeration is mechanized in `verify_J14.py` (12/12 PASS) so any future referee with Python open reproduces the rebuttal in under two seconds. J14 differentiates from J16 by content focus: J16 is the F$_5$-rigid algebra in isolation (target *Algebras and Representation Theory*); J14 is the F$_p$ invariance / non-invariance separation theorem (target *Algebra Universalis*). They share the upstream `tig_dirac.py` reference library but are independent submission targets at different venues.

J13 is the short, *Acta Arithmetica*-targeted cyclotomic-forcing appendix to the Sanders-Gish *Flatness Theorem* (J07, JPAA): under the cyclotomic-embedding calibration (a prime-$p$ closed circle has circumference $p$), the four-structure torus on $\mathbb{Z}/10\mathbb{Z}$ has aspect ratio $R/r = 5/7$ where $R = 5$ is the smallest prime divisor of $10$ at which $A_p = 2\cos(\pi/p)$ is irrational and quadratic ($A_5 = \varphi$, m.p.\ $x^2 - x - 1$) and $r = 7$ is the smallest prime at which $A_p$ first becomes cubic ($A_7$ has m.p.\ $g(x) = x^3 - x^2 - 2x + 1$). The retitle "*Up to a Calibration Choice*" makes explicit that the forcing is *conditional* on the cyclotomic-embedding calibration imported from J07 — a calibration-free derivation would make $5/7$ unconditional and is recorded as Open question (b). Two material errata against an earlier draft are fixed in this version: (M1) the minimal polynomial of $A_7$ over $\mathbb{Q}$ is $g(x) = x^3 - x^2 - 2x + 1$, *not* $h(x) = 8x^3 - 4x^2 - 4x + 1$ — the latter is the m.p.\ of $\cos(\pi/7)$ without the factor 2; the substitution $h(x/2) = g(x)$ encodes the cos-vs-$2\cos$ calibration bridge that gives the paper its title; (M2) Lemma 4.3 of the original draft mis-evaluated the rational-root cubic and has been rewritten to use the correct rational-root test $g(1) = -1$, $g(-1) = 1$ on the correct polynomial. The companion-appearances honesty pass (§6) reorganized the prior six "derivations" of $5/7$: two were reformulations of the same cyclotomic forcing (so labeled), two are independent appearances of the same threshold (not derivations of the ratio itself — the $\sin c^2$ first-G window from J03, the prime-$\pi$-$\varphi$ field bridge), and two earlier "derivations" (a BTQ operator balance and a $5/7 = (\sin(\pi/5)\sin(\pi/7))^?$ bridge) were deleted as not self-contained. The earlier numerical claim that $73/101 = 5/7$ exactly is retracted: $73/101 - 5/7 = 6/707 \approx 1.2\%$, recorded as an open numerical question (whether the discrepancy is an asymptotic correction or genuine independence). The discriminant $\disc(g) = 49 = 7^2$ is a perfect square and the Galois group of $g$ is $A_3 \cong \mathbb{Z}/3\mathbb{Z}$ by the disc-square criterion for cubics, so the splitting field is the totally real cubic subfield of $\mathbb{Q}(\zeta_7)$. The generalization conjecture (Conjecture 6.1) is now scope-restricted to squarefree $n$ with $5 \mid n$ and $n > 5$ — the cases $n = 14, 21$ (with $7 \mid n$ but no prime divisor $\le 5$ with $A_p$ irrational of degree $\le 2$) are explicitly outside the conjecture's domain (Proposition 6.2). The verification script `verify_J13.py` reproduces all algebraic claims at machine precision in under 5 seconds (sympy-based, 6/6 PASS) and explicitly checks both the M1 polynomial identification and the M1 disambiguation $h(A_7) \neq 0$. Drápal-Wanless 2021 is cited as domain precedent (small finite commutative non-associative structures; opposite extremum). J13 differentiates from J15 (the Galois deep cut on the runtime *quartic* $x^4 + 4x^3 - x^2 + 2x - 2$ of the four-core attractor — degree 4, Galois $D_4$, number field LMFDB 4.2.10224.1) by polynomial: J13 is the Galois fact about the *cubic* $x^3 - x^2 - 2x + 1$ of $2\cos(\pi/7)$ — degree 3, Galois $A_3 = \mathbb{Z}/3\mathbb{Z}$, splitting field the totally real cubic subfield of $\mathbb{Q}(\zeta_7)$. They are two different number-field deep cuts on the substrate, both feeding $R/r = T^* = 5/7$ from different angles.

J18 is the $\sigma^{2}$-triadic refinement of a global $-21$ invariant on the residue ring $\mathbb{Z}/10\mathbb{Z}$. Fix the canonical involution $\sigma = (0)(3)(8)(9)(1\,7\,6\,5\,4\,2)$ and the explicit integer-valued function $\Psi_B : \mathbb{Z}/10\mathbb{Z} \to \mathbb{Z}$ tabulated in Table 1 with values $\Psi_B = \{0:+1,\,1:-5,\,2:-3,\,3:-2,\,4:-2,\,5:-1,\,6:-1,\,7:-3,\,8:-3,\,9:-2\}$ (originating as per-element $\mathrm{BH}_{10}$-periods in the corrected $(\mathrm{TS}_8, \mathrm{BH}_{10})$-substrate of J02/J26, treated here as input data). The total $\sum_n \Psi_B(n) = -21$ decomposes in two combinatorially independent ways: as the $\sigma$-orbit triangular split $-T_5 + (-T_3) = -15 + (-6)$ along $\sigma\text{-cycle} \sqcup \sigma\text{-fixed}$ (Theorem 3.1; *table-independent* — follows from $\sigma$ cycle structure plus Table 1 alone), and as the role-Fibonacci split $-F_7 + (-F_6) + (-1) + (+1) = -13 + (-8) + (-1) + 1$ along $F \sqcup S \sqcup T \sqcup V$ (Theorem 3.2; *table-specific* — broken in $0/200$ random commutative tables per the J26 check). Theorem 3.3 records that the two splits cross: $F \cap \sigma\text{-cycle} = \{1,5,7\}$ is neither $\sigma$- nor $\sigma^2$-stable, and no role class contains the 6-cycle. Proposition 4.1 is the R1 sign-swap fix: the two $\sigma^2$-triangular orbits $O_1 = \{1,6,4\}$ and $O_2 = \{7,5,2\}$ carry per-orbit sums $\sum_{O_1}\Psi_B = -8$ and $\sum_{O_2}\Psi_B = -7$ (the previous draft's statement had these reversed; the proof was always correct). The values negate the canonical TIG primes $\{\mathrm{BREATH}=8,\mathrm{HARMONY}=7\}$. The R1 fix also tabulates $\Psi_B$ inline (replacing the prior linear/boundary period formula contradiction) and replaces "conservation/manifestation duality" with Definition 3.4 (table-independent vs. table-specific). All six identities are verified by `verify_J18.py` (six checks: total, $\sigma$-orbit split, role-Fibonacci split, $\sigma^2$ per-orbit values, crossing closure failures, involution sanity data) at machine precision, runtime under one second, standard-library Python only.

---

## §2 — Expected papers when ready

| J# | Title | Target venue | Status |
|---|---|---|---|
| **J24** | Three-Substrate Joint-Closure Chain on Z/10Z | *J. Algebra* | central-theorem paper for the 8-shell chain |

---

## §3 — What lives here when landed

Each paper folder mirrors the working-repo structure:

```
J{NN}/
├── README.md
├── cover_letter.md
├── manuscript/
│   ├── manuscript.tex (or .md)
│   └── verify_*.py
└── SAVE_PLAN_J{NN}.md (optional)
```

All `verify_*.py` scripts here PASS at machine precision at the time the paper landed.

---

## §4 — Domain notes for algebra papers

Algebra papers in this corpus emphasize:

- **Finite ring / group theoretic claims** at the integer or rational level.
- **D₄ Galois structure** (the runtime quartic's symmetry group; LMFDB number field 4.2.10224.1).
- **Wedderburn decomposition** of natural irreps under D₄ action.
- **Joint sub-magma structure** of (TSML, BHML) on Z/10 — the 8-shell chain.

Cross-references:
- [`../../03_canonical_reference/FORMULAS_AND_TABLES.md`](../../03_canonical_reference/FORMULAS_AND_TABLES.md) Volumes B, F, G, H carry the load-bearing algebra.
- [`../../TIG_FROM_THE_GROUND_UP.md`](../../TIG_FROM_THE_GROUND_UP.md) Parts 3–7 are the algebra tutorial.

---

*7SiTe Public Sovereignty License v2.1 — see [`../../LICENSE`](../../LICENSE).*
